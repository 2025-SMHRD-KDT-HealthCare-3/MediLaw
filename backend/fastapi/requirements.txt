# 웹 + MCP를 한 서버(uvicorn)에서 함께 돌리기 위한 호환 세트.
# 주의: mcp(sse-starlette)는 starlette>=0.49 를 요구 → fastapi도 starlette 1.x 허용 버전이어야 함.
#       (구 fastapi 0.115 + 신 mcp 조합은 starlette 핀 충돌로 깨짐)
fastapi==0.137.0
starlette==1.3.1
uvicorn[standard]==0.49.0
python-multipart==0.0.12
aiofiles==24.1.0
pydantic==2.13.4
pypdf==6.13.0
pypdfium2==5.10.1
Pillow==12.2.0
python-dotenv==1.0.1

# 임베딩 / 하이브리드 검색 + 생성(gpt-5.5)
# 주의: gpt-5.5 / reasoning_effort(추론 강도) 파라미터는 openai>=2.x 필요.
#       1.57.0 등 구버전은 reasoning_effort 미지원 → 챗봇 생성 호출이 깨짐.
openai==2.41.1
numpy==2.2.0          # 벡터 코사인 폴백(full DB 전용). slim DB엔 embedding 컬럼이 없어 폴백 불가.
# ⚠️ sqlite-vec 필수: 배포되는 medilaw.db 는 slim(용량 축소) 버전이라 벡터가 chunk_vec 에만
#    저장됨. sqlite-vec 가 없으면 벡터검색 불가 → FTS(키워드) 전용으로 강등됨(에러는 아님).
sqlite-vec==0.1.6

# 기능 4 — MCP Server (FastAPI에 /mcp SSE로 마운트)
mcp==1.27.2
sse-starlette==3.4.4

# PDF 능동형 에디터 — 디지털 추출(표/레이아웃)
pdfplumber==0.11.10

# ─ PDF 스캔본 OCR 백엔드(OCR_BACKEND=paddleocr_vl)용 선택 의존성 ─────────────
# 무겁고(≈1.5GB+) 모델 로딩에 RAM 6~8GB+(권장 GPU)가 필요해 기본 설치에서 제외.
# 스캔본 OCR을 자체호스팅(국내처리)하려면 아래를 설치:
#   pip install paddlepaddle==3.3.1 "paddleocr[doc-parser]==3.7.0" "paddlex[ocr]==3.7.1"
# 미설치 시: 디지털 PDF는 정상, 스캔본 OCR은 빈 결과(graceful). 클라우드 대안은 OCR_BACKEND=vision.
